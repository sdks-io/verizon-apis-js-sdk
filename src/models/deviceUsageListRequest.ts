/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { lazy, object, optional, Schema, string } from '../schema';
import { DeviceId, deviceIdSchema } from './deviceId';

/** Request to return the daily network data usage of a single device during a specified time period. */
export interface DeviceUsageListRequest {
  /** An identifier for a single device. */
  deviceId?: DeviceId;
  /** The earliest date for which you want usage data. */
  earliest?: string;
  /** The last date for which you want usage data. */
  latest?: string;
}

export const deviceUsageListRequestSchema: Schema<DeviceUsageListRequest> = object(
  {
    deviceId: ['deviceId', optional(lazy(() => deviceIdSchema))],
    earliest: ['earliest', optional(string())],
    latest: ['latest', optional(string())],
  }
);
