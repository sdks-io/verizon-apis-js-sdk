/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { array, lazy, object, optional, Schema, string } from '../schema';
import { Device, deviceSchema } from './device';

/** Request body to Performs a device reboot. */
export interface DeviceResetRequest {
  /** The name of the account. An account name is usually numeric, and must include any leading zeros. */
  accountName?: string;
  /** The action you want to take on the device. */
  action?: string;
  /** The devices for which you want to perform a factory reset or reboot. */
  devices?: Device[];
}

export const deviceResetRequestSchema: Schema<DeviceResetRequest> = object({
  accountName: ['accountName', optional(string())],
  action: ['action', optional(string())],
  devices: ['devices', optional(array(lazy(() => deviceSchema)))],
});
