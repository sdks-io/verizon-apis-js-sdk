/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { object, optional, Schema, string } from '../schema';

/** Default location where service needs to be deployed. */
export interface DefaultLocation {
  /** CSP region where service needs to be deployed. */
  region?: string;
  /** Zone within a region where service needs to be deployed. */
  zone?: string;
}

export const defaultLocationSchema: Schema<DefaultLocation> = object({
  region: ['region', optional(string())],
  zone: ['zone', optional(string())],
});
