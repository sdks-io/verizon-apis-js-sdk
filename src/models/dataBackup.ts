/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { lazy, object, optional, Schema } from '../schema';
import { AccessKey, accessKeySchema } from './accessKey';
import {
  DataBackupProviderEnum,
  dataBackupProviderEnumSchema,
} from './dataBackupProviderEnum';
import { Role, roleSchema } from './role';

export interface DataBackup {
  /** Types of data backup provider. */
  provider?: DataBackupProviderEnum;
  accessKey?: AccessKey;
  /** Role of the user calling API. */
  role?: Role;
}

export const dataBackupSchema: Schema<DataBackup> = object({
  provider: ['provider', optional(dataBackupProviderEnumSchema)],
  accessKey: ['accessKey', optional(lazy(() => accessKeySchema))],
  role: ['role', optional(lazy(() => roleSchema))],
});
