/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  boolean,
  lazy,
  number,
  object,
  optional,
  Schema,
  string,
} from '../schema';
import {
  AccountLicenseDeviceListItem,
  accountLicenseDeviceListItemSchema,
} from './accountLicenseDeviceListItem';

/** Account license information. */
export interface AccountLicenseInfo {
  /** Account identifier in "##########-#####". */
  accountName?: string;
  /** Number of monthly licenses in an MRC subscription. */
  totalLicenses?: number;
  /** Number of licenses currently assigned to devices. */
  assignedLicenses?: number;
  /** True if there are more devices to retrieve. */
  hasMoreData?: boolean;
  /** If hasMoreData=true, the startIndex to use for the next request. 0 if hasMoreData=false. */
  lastSeenDeviceId?: number;
  /** The list of devices that have licenses assigned, including the date and time of when each license was assigned. */
  deviceList?: AccountLicenseDeviceListItem[];
}

export const accountLicenseInfoSchema: Schema<AccountLicenseInfo> = object({
  accountName: ['accountName', optional(string())],
  totalLicenses: ['totalLicenses', optional(number())],
  assignedLicenses: ['assignedLicenses', optional(number())],
  hasMoreData: ['hasMoreData', optional(boolean())],
  lastSeenDeviceId: ['lastSeenDeviceId', optional(number())],
  deviceList: [
    'deviceList',
    optional(array(lazy(() => accountLicenseDeviceListItemSchema))),
  ],
});
