/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { object, Schema, string } from '../schema';
import {
  CampaignMetaInfoProtocolEnum,
  campaignMetaInfoProtocolEnumSchema,
} from './campaignMetaInfoProtocolEnum';

/** Available firmware. */
export interface FirmwarePackage {
  /** Firmware name. */
  firmwareName: string;
  /** Firmware from version. */
  firmwareFrom: string;
  /** Firmware to version. */
  firmwareTo: string;
  /** Firmware launch date. */
  launchDate: string;
  /** Firmware release note. */
  releaseNote: string;
  /** Firmware applicable device model. */
  model: string;
  /** Firmware applicable device make. */
  make: string;
  /** Firmware protocol. Valid values include: LWM2M, OMD-DM. */
  protocol: CampaignMetaInfoProtocolEnum;
}

export const firmwarePackageSchema: Schema<FirmwarePackage> = object({
  firmwareName: ['firmwareName', string()],
  firmwareFrom: ['firmwareFrom', string()],
  firmwareTo: ['firmwareTo', string()],
  launchDate: ['launchDate', string()],
  releaseNote: ['releaseNote', string()],
  model: ['model', string()],
  make: ['make', string()],
  protocol: ['protocol', campaignMetaInfoProtocolEnumSchema],
});
