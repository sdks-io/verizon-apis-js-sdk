/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  boolean,
  lazy,
  object,
  optional,
  Schema,
  string,
} from '../schema';
import { V2SoftwareInfo, v2SoftwareInfoSchema } from './v2SoftwareInfo';

/** Account device information. */
export interface V2AccountDevice {
  /** Device identifier. */
  deviceId: string;
  /** MDN. */
  mdn: string;
  /** Device model. */
  model: string;
  /** Device make. */
  make: string;
  /** Device FOTA capable. */
  fotaEligible: boolean;
  /** Device application FOTA capable. */
  appFotaEligible: boolean;
  /** License assigned device. */
  licenseAssigned: boolean;
  /** LWM2M, OMD-DM or HTTP. */
  distributionType: string;
  /** List of sofware. */
  softwareList: V2SoftwareInfo[];
  /** The date and time of when the device is created. */
  createTime?: string;
  /** The date and time of when the device firmware or software is upgraded. */
  upgradeTime?: string;
  /** The date and time of when the device is updated. */
  updateTime?: string;
  /** The date and time of when the device is refreshed. */
  refreshTime?: string;
}

export const v2AccountDeviceSchema: Schema<V2AccountDevice> = object({
  deviceId: ['deviceId', string()],
  mdn: ['mdn', string()],
  model: ['model', string()],
  make: ['make', string()],
  fotaEligible: ['fotaEligible', boolean()],
  appFotaEligible: ['appFotaEligible', boolean()],
  licenseAssigned: ['licenseAssigned', boolean()],
  distributionType: ['distributionType', string()],
  softwareList: ['softwareList', array(lazy(() => v2SoftwareInfoSchema))],
  createTime: ['createTime', optional(string())],
  upgradeTime: ['upgradeTime', optional(string())],
  updateTime: ['updateTime', optional(string())],
  refreshTime: ['refreshTime', optional(string())],
});
