/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  bigint,
  lazy,
  object,
  optional,
  Schema,
  string,
} from '../schema';
import { CustomFields, customFieldsSchema } from './customFields';

/** Details of the service plan. */
export interface ServicePlan {
  /** The code that is used by the carrier for the service plan. */
  carrierServicePlanCode?: string;
  /** The code of the service plan, which may not be the same as the name. */
  code?: string;
  /** Any extended attributes for the service plan, as Key and Value pairs. */
  extendedAttributes?: CustomFields[];
  /** The name of the service plan. */
  name?: string;
  /** The size of the service plan in kilobytes. */
  sizeKb?: bigint;
}

export const servicePlanSchema: Schema<ServicePlan> = object({
  carrierServicePlanCode: ['carrierServicePlanCode', optional(string())],
  code: ['code', optional(string())],
  extendedAttributes: [
    'extendedAttributes',
    optional(array(lazy(() => customFieldsSchema))),
  ],
  name: ['name', optional(string())],
  sizeKb: ['sizeKb', optional(bigint())],
});
