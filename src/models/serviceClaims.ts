/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { array, lazy, number, object, optional, Schema } from '../schema';
import { Claim, claimSchema } from './claim';

/** Response to get all claims. */
export interface ServiceClaims {
  /** Count for all the claims returned after hitting the API. */
  count?: number;
  /** List of all claims. */
  claimsResList?: Claim[];
}

export const serviceClaimsSchema: Schema<ServiceClaims> = object({
  count: ['count', optional(number())],
  claimsResList: ['claimsResList', optional(array(lazy(() => claimSchema)))],
});
