/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { bigint, object, optional, Schema, string } from '../schema';

/** Resource Base of the service. */
export interface ResourceBase {
  /** Resource unit ex :MB. */
  unit?: string;
  /** Resource value e.g. 200MB. */
  value?: bigint;
  /** Resource max value e.g. 400MB. */
  max?: bigint;
  /** Resource min value e.g. 10MB. */
  min?: bigint;
}

export const resourceBaseSchema: Schema<ResourceBase> = object({
  unit: ['unit', optional(string())],
  value: ['value', optional(bigint())],
  max: ['max', optional(bigint())],
  min: ['min', optional(bigint())],
});
