/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  bigint,
  boolean,
  lazy,
  object,
  optional,
  Schema,
  string,
} from '../schema';
import { AwsCspProfile, awsCspProfileSchema } from './awsCspProfile';
import { AzureCspProfile, azureCspProfileSchema } from './azureCspProfile';
import {
  CSPProfileTypeEnum,
  cSPProfileTypeEnumSchema,
} from './cSPProfileTypeEnum';
import { DefaultLocation, defaultLocationSchema } from './defaultLocation';

/** The user can create cloud credentials used for deploying workloads to the CSP environment. */
export interface CSPProfile {
  /** System generated unique identifier to identify the CSP Profile uniquely. */
  id?: string;
  /** Usage tells how many services are using the CSP Profile. Only CSP Profile with 0 usage count be allowed to delete. */
  usage?: bigint;
  /** Name of the cloud credential to uniquely identify the CSP. */
  cspProfileName: string;
  /** Unique identification of the organization creating the CSP Profile. */
  customerID?: string;
  /** Project name where service artifacts needs to be stored. */
  projectName?: string;
  /** Type of CSP profile. */
  type?: CSPProfileTypeEnum;
  /** Information related to manage resources in AWS infrastructure. */
  awsCspProfile?: AwsCspProfile;
  /** Information related to manage resources in Azure infrastructure. */
  azureCspProfile?: AzureCspProfile;
  /** Default location where service needs to be deployed. */
  defaultLocation?: DefaultLocation;
  /** Auto-derived Time of creation. Part of response only. */
  verificationTimeStamp?: string;
  /** State of the CSP profile. */
  state?: string;
  /** True if CSP is validated using provided credential, false otherwise. */
  isValidated?: boolean;
  /** Auto-derived Time of creation. Part of response only. */
  createdDate?: string;
  /** Last modified time. Part of response only. */
  lastModifiedDate?: string;
  /** User who created the dropDown. Part of response only. */
  createdBy?: string;
  /** User who last modified the dropDown. Part of response only. */
  lastModifiedBy?: string;
}

export const cSPProfileSchema: Schema<CSPProfile> = object({
  id: ['id', optional(string())],
  usage: ['usage', optional(bigint())],
  cspProfileName: ['cspProfileName', string()],
  customerID: ['customerID', optional(string())],
  projectName: ['projectName', optional(string())],
  type: ['type', optional(cSPProfileTypeEnumSchema)],
  awsCspProfile: ['awsCspProfile', optional(lazy(() => awsCspProfileSchema))],
  azureCspProfile: [
    'azureCspProfile',
    optional(lazy(() => azureCspProfileSchema)),
  ],
  defaultLocation: [
    'defaultLocation',
    optional(lazy(() => defaultLocationSchema)),
  ],
  verificationTimeStamp: ['verificationTimeStamp', optional(string())],
  state: ['state', optional(string())],
  isValidated: ['isValidated', optional(boolean())],
  createdDate: ['createdDate', optional(string())],
  lastModifiedDate: ['lastModifiedDate', optional(string())],
  createdBy: ['createdBy', optional(string())],
  lastModifiedBy: ['lastModifiedBy', optional(string())],
});
