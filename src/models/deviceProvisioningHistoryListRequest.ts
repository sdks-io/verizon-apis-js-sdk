/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { lazy, object, optional, Schema, string } from '../schema';
import { DeviceId, deviceIdSchema } from './deviceId';

/** Request to return the provisioning history of a specified device during a specified time period. */
export interface DeviceProvisioningHistoryListRequest {
  /** An identifier for a single device. */
  deviceId?: DeviceId;
  /** The earliest date and time for which you want provisioning data. */
  earliest?: string;
  /** The last date and time for which you want provisioning data. */
  latest?: string;
}

export const deviceProvisioningHistoryListRequestSchema: Schema<DeviceProvisioningHistoryListRequest> = object(
  {
    deviceId: ['deviceId', optional(lazy(() => deviceIdSchema))],
    earliest: ['earliest', optional(string())],
    latest: ['latest', optional(string())],
  }
);
